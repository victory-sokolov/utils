pre-commit:
  commands:
    format-check:
      run: bun run format
    lint:
      run: bun run lint
    typecheck:
      run: bun run typecheck

commit-msg:
  commands:
    commitlint:
      run: bunx --no -- commitlint --edit {1}
